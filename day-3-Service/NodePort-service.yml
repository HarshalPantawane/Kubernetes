apiVersion: v1
kind: Service
metadata:
  name: busybox-service
  namespace: default

spec:
  type: NodePort  
  selector:
    app: myapp
  ports:
   - protocol: TCP
     port: 80
     targetPort: 80
     NodePort: 30080  # optional

# kubectl apply -f NodePort-service.yml
# kubectl get svc