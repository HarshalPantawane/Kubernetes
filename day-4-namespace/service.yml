apiVersion: v1 
kind: Service
metadata: 
  name: nginx-servcie
spec:  
  type: ClusterIP     # default service type
  selector:
    app: myapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80     